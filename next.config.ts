import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "dummyimage.com",
      "images.prismic.io",
      "images.unsplash.com",
      "aceternity.com",
    ],
  },
};

export default nextConfig;
